const StarWarDisplay = ({ name,title}) => {
  // console.log(starWarsData)
  return (
    <div className="list">
      <h3>{name}</h3>
      {/* <h4>{title}</h4> */}
    </div>
  );
};
export default StarWarDisplay;
